var assert = require('assert');

var serverUrl = 'http://localhost:8080';
var endpoint = '/api/convert';

describe('JSON to CSV', function () {

  describe('API', function () {
 
    it('should have a RESTful endpoint', function (done) {
      assert.equal(false, true);
    }); 

    it('should accept post request with JSON', function (done) {
      assert.equal(false, true);
    });

  });

  describe('CSV formatting', function () {

    it('should validate that JSON can be transformed into CSV', function (done) {
      assert.equal(false, true);
    }); 

    it('should return CSV formatted string for valid JSON', function (done) {
      assert.equal(false, true);
    });

  });

});
